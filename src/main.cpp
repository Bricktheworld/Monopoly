#include <vultr.hpp>
#include <sandbox_test.h>
#include <component_registrar.h>
#include <exports.h>

using namespace Vultr;
int main(void)
{
    // auto *e = new Engine();

    // float lastTime = 0;
    // engine_init(e, false);
    // engine_load_game(e, static_cast<Game *>(init(e)));

    // change_world(e, new_world(e->component_registry));
    // register_components(e);

    // engine_init_game(e);

    // while (!glfwWindowShouldClose(e->window))
    // {
    //     engine_update_game(e, lastTime, true);
    //     glfwSwapBuffers(e->window);
    //     glfwPollEvents();
    // }

    // return 0;
}
